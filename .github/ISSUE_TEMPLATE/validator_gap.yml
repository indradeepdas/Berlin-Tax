name: Validator gap
description: Report a missing deterministic validation.
title: "[Validator]: "
labels: ["scripts", "validation"]
body:
  - type: textarea
    id: gap
    attributes:
      label: Gap
      description: What should be validated deterministically?
    validations:
      required: true
  - type: textarea
    id: example
    attributes:
      label: Example input
      description: Provide a minimal redacted example if possible.
    validations:
      required: false

